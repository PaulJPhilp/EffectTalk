/**
 * Re-exports from effect-schema-utils for backward compatibility
 *
 * @deprecated Use `effect-schema-utils` directly instead
 * @packageDocumentation
 */

export {
  formatParseError,
  extractFieldPath,
  extractExpected,
  extractActual,
  getErrorMessage,
  extractErrorDetails,
  type ParseErrorDetails,
} from "effect-schema-utils";
